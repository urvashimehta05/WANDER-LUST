<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css"/>
    <title>Wanderlust</title>
    <style>
      .form-label{
              font-size: 16px;
      }
    </style>
  </head>
  <body>
<div class="row mt-3">
  <div class="col-8 offset-3 ">
    <h3> <b><p class="card-text" imagee><%= listing.title %></b> </h3>
    </div>
    <div class="card col-6 offset-3 show-img listing-card">
      <% if (listing.image && listing.image.url) { %>
        <img 
          src="<%= listing.image.url %>"
          class="card-img-top show-img"
          alt="Listing Image"
        >
      <% } else { %>
        <img 
          src="https://images.unsplash.com/photo-1578645510447-e20b4311e3ce?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxzZWFyY2h8NDF8fGNhbXBpbmd8ZW58MHx8MHx8fDA%3D&auto=format&fit=crop&w=800&q=60"
          class="card-img-top show-img"
          alt="Default Image"
        >
      <% } %>
      <div class="card-body">
        <%= listing.description %><br>
        &#8377; <%= listing.price.toLocaleString("en-IN") %></li><br>
        <%= listing.location %><br>
        <%= listing.country %><br>
        </p>
        </div>
        </div>
        </div>
  
    <br />
    <div class="btns">
    <form method="GET" action="/listings/<%= listing._id %>/edit"><button class="btn btn-dark cols-8 offset-3 edit-btn">Edit</button></form>
    <br /><br />
    <form method="POST" action="/listings/<%=listing._id%>?_method=DELETE">
      <button class="btn btn-dark offset-4 ">Delete</button></div>
    </form>
    </body>
    <div class="col-8 offset-3 mb-3">
      <hr>
      <h4>Leave a Review</h4>
       <form method="POST" novalidate 
       class="needs-validation"
       action="/listings/<%= listing.id %>/reviews">
        <div class="mb-3 mt-3">
          <label for="rating" class="form-label">Rating</label>
          <input type="range"min="1" max="5" id="rating" name="review[rating]" class="form-range">
        </div>
        <div class="mb-3 mt-3">
          <label for="comments" class="form-label">Comments</label>
          <textarea name="review[comments]" id="comments" class="form-control" cols="30" rows="5" required></textarea>
          <div class="invalid-feedback">Please add some comments for reviews</div>
        </div>
      <button class="btn btn-outline-dark">Submit</button>
      </form>
      <hr>
      <h4><b>All Reviews</b></h4>
      <div class="row">
      <% for(review of listing.reviews) { %>
        <div class="card col-5 ms-3 mb-3">
      <div class="class-body">
        <h5 class="card-title">
         Jane doe
        </h5>
        <p class="card-text"><%= review.comments %></p>
        <p class="card-text"><%= review.rating %> stars</p>
      </div>
      <form class="mb-3" method="POST" action="/listing/<%= listing._id %>/reviews/<%=review._id %>?_method=DELETE">
        <button class="btn btn-sm btn-dark">Delete</button>
      </form>
     </div>
     <% } %>
</div>
  </div>
</div>
  </body>
</html>